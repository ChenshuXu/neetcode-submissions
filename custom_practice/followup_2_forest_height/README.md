# Follow-up 2 — Maximum N-ary Forest Height

Given `List[Node]` returned by a deletion problem, return the maximum height across the forest.

- Height counts nodes.
- An empty forest has height `0`.
- A leaf has height `1`.
- Do not mutate the forest.

Implement `max_forest_depth` in `solution.py`, then run:

```bash
python3 custom_practice/followup_2_forest_height/run_tests.py
```
